{
  "name": "orderflow",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo do OrderFlow com backend Fastify, frontend React/Vite e MariaDB.",
  "scripts": {
    "dev": "sh -c 'npm run dev:backend & npm run dev:frontend & wait'",
    "start": "npm run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend/app run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend/app run build",
    "lint": "npm --prefix frontend/app run lint",
    "install:all": "npm install --prefix backend && npm install --prefix frontend/app",
    "db:up": "docker compose up -d database",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f database",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "prisma:migrate": "npm --prefix backend run prisma:migrate"
  },
  "workspaces": [
    "backend",
    "frontend/app"
  ]
}
